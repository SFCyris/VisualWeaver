# Replace visualweaver.example.com with your domain. Caddy fetches and renews a
# Let's Encrypt certificate automatically and reverse-proxies to VisualWeaver on
# the internal compose network.
visualweaver.example.com {
	reverse_proxy visualweaver:8420
}
